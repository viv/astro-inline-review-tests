{
  "name": "astro-inline-review-tests",
  "version": "0.0.1",
  "private": true,
  "description": "Playwright acceptance tests for astro-inline-review",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
